import { CapitalTextPipe } from './capital-text.pipe';

describe('CapitalTextPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalTextPipe();
    expect(pipe).toBeTruthy();
  });
});
